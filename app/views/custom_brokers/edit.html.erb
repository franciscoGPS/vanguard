<%- model_class = CustomBroker -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= render 'form' %>

<%= link_to 'Show', greenhouse_custom_broker_path(@greenhouse.id, @custom_broker.id) %> |
<%= link_to 'Back', greenhouse_custom_brokers_path(@greenhouse.id) %>
