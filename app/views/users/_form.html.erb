<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>

      


    </div>
  <% end %>


      <%= f.label :email %>
      <%= f.text_field :email%>

      <%= f.label :name %>
      <%= f.text_field :name %>

      
      <%= f.collection_select :role_id, Role.all ,:id,:name, {prompt: "Selecciona uno, pendjo"} %>
      

      <%= f.label :job %>
      <%= f.text_field :job %>

      <%= f.label :phone %>
      <%= f.text_field :phone %>

      <%= f.label :admin %>
      <%= f.check_box :admin %>
      


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
