<%= form_for(@count_type) do |f| %>
  <% if @count_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@count_type.errors.count, "error") %> prohibited this count_type from being saved:</h2>

      <ul>
      <% @count_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
