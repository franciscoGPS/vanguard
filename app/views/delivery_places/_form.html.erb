<%= form_for(@delivery_place) do |f| %>
  <% if @delivery_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@delivery_place.errors.count, "error") %> prohibited this delivery_place from being saved:</h2>

      <ul>
      <% @delivery_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
